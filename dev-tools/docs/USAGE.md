# 📖 Как пользоваться проектом

## 🚀 Ежедневный запуск

### Вариант 1: Локальные домены (рекомендуется)

```bash
# 1. Запустить все сервисы
npm run dev

# 2. Открыть в браузере
# Main App:    http://recipes.local
# Admin Panel: http://admin.recipes.local
# API Swagger: http://api.recipes.local/swagger
```

### Вариант 2: Через порты (если nginx выключен)

```bash
# 1. Запустить все сервисы
npm run dev

# 2. Открыть в браузере
# Main App:    http://localhost:3000
# Admin Panel: http://localhost:5173
# API Swagger: http://localhost:5010/swagger
```

---

## 🔐 Учетные данные

**Администратор:**
- Данные находятся в файле `.credentials` (не в git)
- Если файла нет, создай админа: `./create-admin.sh`

**Войти в админ-панель:**
- http://admin.recipes.local → Login

---

## 🛠 Управление nginx

### Проверить статус
```bash
brew services list | grep nginx
```

### Запустить nginx
```bash
brew services start nginx
```

### Остановить nginx
```bash
brew services stop nginx
```

### Перезапустить nginx (после изменения конфигов)
```bash
brew services restart nginx
```

### Посмотреть логи
```bash
# Логи ошибок
tail -f /opt/homebrew/var/log/nginx/error.log

# Логи доступа
tail -f /opt/homebrew/var/log/nginx/access.log
```

---

## 🔄 Типичные сценарии

### Начало рабочего дня
```bash
# Запустить все сервисы
npm run dev

# Открыть приложение
open http://recipes.local
```

### Что-то не работает / странные ошибки
```bash
# Перезапустить все сервисы
./restart-dev.sh
npm run dev
```

### Нужен чистый старт (сбросить базу данных)
```bash
# Внимание: удалит все данные!
./reset-users.sh

# Потом запустить сервисы
npm run dev
```

### Создать нового администратора
```bash
# С дефолтным паролем
./create-admin.sh admin2@recipes.local admin2 password123

# Или просто
./create-admin.sh
```

### Доступ с телефона/планшета в той же сети

**Проблема:** На iOS/Android нельзя редактировать hosts файл

**Решение:** Используй IP с портом
```
http://192.168.1.14:3000       → Main App
http://192.168.1.14:5173       → Admin Panel
http://192.168.1.14:5010       → API
```

---

## 📱 Работа с PWA (установка на телефон)

### На iPhone/iPad:
1. Открой http://192.168.1.14:3000 в Safari
2. Нажми кнопку "Поделиться" (квадрат со стрелкой)
3. Выбери "На экран «Домой»"
4. Нажми "Добавить"

### На Android:
1. Открой http://192.168.1.14:3000 в Chrome
2. Нажми меню (три точки)
3. Выбери "Установить приложение" или "Добавить на главный экран"

После установки приложение будет работать офлайн (кеш на 24 часа).

---

## 🐛 Решение проблем

### "502 Bad Gateway" в nginx
**Причина:** Dev серверы не запущены
**Решение:**
```bash
npm run dev
```

### "Cannot connect to recipes.local"
**Причина:** nginx не запущен
**Решение:**
```bash
brew services start nginx
```

### Страница загружается бесконечно
**Причина:** Старые процессы заняли порты
**Решение:**
```bash
./restart-dev.sh
npm run dev
```

### Ошибка подключения к базе данных
**Причина:** PostgreSQL не запущен
**Решение:**
```bash
brew services start postgresql@16
```

### Порт уже занят (EADDRINUSE)
**Причина:** Старый процесс не остановился
**Решение:**
```bash
# Найти и убить процесс на порту 3000
lsof -ti:3000 | xargs kill -9

# Или использовать скрипт
./restart-dev.sh
```

### HMR (Hot Reload) не работает
**Причина:** WebSocket не проксируется
**Решение:** Уже настроено в nginx.conf, но если не работает:
```bash
# Перезапустить nginx
brew services restart nginx

# Перезапустить dev серверы
./restart-dev.sh
npm run dev
```

---

## 📦 Дополнительные команды

### Сборка продакшн версии
```bash
npm run build
```

### Запуск только API
```bash
npm run dev:api
```

### Запуск только Nuxt app
```bash
npm run dev:app
```

### Запуск только React admin
```bash
npm run dev:admin
```

### Docker (для деплоя)
```bash
# Запустить все в Docker
docker-compose up -d

# Остановить
docker-compose down

# Посмотреть логи
docker-compose logs -f
```

---

## 📚 Документация

- **START.md** - Быстрый старт проекта
- **SCRIPTS.md** - Все доступные скрипты
- **LOCAL-DOMAIN.md** - Подробно про nginx и домены
- **CLAUDE.md** - Техническая документация проекта
- **USAGE.md** (этот файл) - Как пользоваться

---

## 💡 Полезные советы

### 1. Всегда используй домены в разработке
- ✅ `http://recipes.local` - красиво, профессионально
- ❌ `http://localhost:3000` - старый способ

### 2. После pull из Git
```bash
# Обновить зависимости
cd frontend/app && npm install
cd ../admin && npm install
cd ../../

# Применить миграции базы
cd backend/RecipesApi && dotnet ef database update
cd ../../

# Запустить
npm run dev
```

### 3. Перед коммитом изменений
```bash
# Убедись что все работает
npm run build

# Проверь что API работает
curl http://api.recipes.local/api/health
```

### 4. Добавление нового администратора
```bash
# Создать через скрипт
./create-admin.sh newadmin@recipes.local newadmin password123

# Или через Swagger
# Открой: http://api.recipes.local/swagger
# 1. POST /api/auth/register - зарегистрировать
# 2. POST /api/admin/make-admin/{userId} - дать права
```

### 5. Экспорт/импорт базы данных
```bash
# Экспорт
pg_dump -h localhost -U postgres recipes_db > backup.sql

# Импорт
psql -h localhost -U postgres recipes_db < backup.sql
```

---

## 🎯 Быстрые ссылки

После запуска `npm run dev`:

- 🌐 **Main App:** http://recipes.local
- 👨‍💼 **Admin Panel:** http://admin.recipes.local
- 🔌 **API:** http://api.recipes.local
- 📖 **Swagger:** http://api.recipes.local/swagger

**С телефона (в той же WiFi сети):**
- 📱 **Main App:** http://192.168.1.14:3000
- 📱 **Admin Panel:** http://192.168.1.14:5173
- 📱 **API:** http://192.168.1.14:5010
