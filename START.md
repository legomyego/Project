# 🚀 Быстрый старт проекта

> 💡 **Документация:**
> - [USAGE.md](USAGE.md) - Как пользоваться проектом (начни отсюда!)
> - [SCRIPTS.md](SCRIPTS.md) - Все доступные скрипты
> - [LOCAL-DOMAIN.md](LOCAL-DOMAIN.md) - Настройка локальных доменов

## 🌐 Локальный домен (опционально, но круто!)

Вместо портов можно настроить красивые домены:

```bash
./setup-local-domain.sh
```

После этого доступ по:
- **Main App:** http://recipes.local
- **Admin Panel:** http://admin.recipes.local
- **API:** http://api.recipes.local

> Требуется nginx (устанавливается автоматически через Homebrew)

---

## Способ 1: Локальная разработка (рекомендуется для разработки)

### Предварительные требования:
- Node.js 20+
- .NET 9 SDK
- PostgreSQL (запущен на localhost:5432)

### Запуск одной командой:

```bash
npm run dev
```

Это запустит все 3 сервиса одновременно:
- **Backend API** на http://localhost:5010
- **Nuxt App** на http://localhost:3000
- **React Admin** на http://localhost:5173

### Другие команды:

```bash
# Собрать все проекты
npm run build

# Запустить только API
npm run dev:api

# Запустить только Nuxt app
npm run dev:app

# Запустить только React admin
npm run dev:admin
```

---

## Способ 2: Docker Compose (рекомендуется для продакшена)

### Первый запуск:

1. **Создайте .env файл:**
```bash
cp .env.example .env
# Отредактируйте .env и установите JWT_SECRET и пароли
```

2. **Запустите все сервисы:**
```bash
docker-compose up -d
```

3. **Проверьте логи:**
```bash
docker-compose logs -f
```

### Доступ к приложению через Nginx:
- **Main App:** http://localhost
- **Admin Panel:** http://localhost/admin
- **API:** http://localhost/api

### Управление Docker контейнерами:

```bash
# Запустить все сервисы
npm run docker:up

# Остановить все сервисы
npm run docker:down

# Посмотреть логи
npm run docker:logs

# Пересобрать и запустить
npm run docker:rebuild
```

---

## Способ 3: Скрипт для macOS (открывает вкладки Terminal)

```bash
./start-dev.sh
```

Этот скрипт откроет 3 вкладки Terminal, каждая с отдельным сервисом.

---

## Первый запуск (настройка базы данных)

### Для локальной разработки:

1. **Убедитесь, что PostgreSQL запущен:**
```bash
pg_isready -h localhost -p 5432
```

2. **Создайте базу данных:**
```bash
createdb recipesdb
```

3. **Примените миграции:**
```bash
cd backend/RecipesApi
dotnet ef database update
```

### Для Docker:

База данных создается автоматически при запуске `docker-compose up`.

---

## Тестовые данные

После запуска API автоматически создаст тестовые данные:
- 5 рецептов с разными ценами
- Вы можете зарегистрировать нового пользователя через `/register`

---

## Порты

| Сервис | Локально | Docker (через Nginx) |
|--------|----------|---------------------|
| Nuxt App | :3000 | / |
| React Admin | :5173 | /admin |
| Backend API | :5010 | /api |
| PostgreSQL | :5432 | :5432 |
| Swagger | :5010/swagger | /api/swagger |

---

## Полезные ссылки

- **Swagger API Docs:** http://localhost:5010/swagger
- **GitHub Actions:** автоматический CI/CD при push
- **CLAUDE.md:** полная документация проекта

---

## Остановка сервисов

### Локальная разработка:
Нажмите `Ctrl+C` в терминале где запущен `npm run dev`

### Docker:
```bash
docker-compose down
```

---

## Troubleshooting

### PostgreSQL не запущен
```bash
# macOS (Homebrew)
brew services start postgresql@16

# Docker
docker-compose up db -d
```

### Порт уже занят
Если порт 3000/5010/5173 уже используется, остановите процесс:
```bash
lsof -ti:3000 | xargs kill -9
```

### Ошибка миграций
```bash
cd backend/RecipesApi
dotnet ef database drop
dotnet ef database update
```
