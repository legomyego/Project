# 📋 Итоговая настройка проекта

## ✅ Что было сделано в этой сессии

### 1. 🚀 Упрощен запуск проекта

**Создано:**
- `package.json` в корне — запуск всех сервисов одной командой
- `npm run dev` — запускает API, Nuxt и Admin параллельно
- `start-dev.sh` — открывает отдельные вкладки Terminal (macOS)
- `restart-dev.sh` — перезапуск с очисткой портов

**Использование:**
```bash
npm run dev
```

---

### 2. 🌐 Настроены локальные домены

**Вместо портов:**
- ~~http://localhost:3000~~ → **http://recipes.local**
- ~~http://localhost:5173~~ → **http://admin.recipes.local**
- ~~http://localhost:5010~~ → **http://api.recipes.local**

**Файлы:**
- `nginx-local.conf` — конфигурация nginx с CORS
- `setup-local-domain.sh` — автоматическая установка
- `/etc/hosts` — резолвинг доменов

**Ключевые моменты:**
- nginx проксирует `recipes.local/api/*` → `localhost:5010`
- CORS настроен правильно: `Access-Control-Allow-Origin: http://recipes.local`
- WebSocket поддержка для HMR (Hot Module Replacement)

---

### 3. 🔐 Настроена безопасность

**Учетные данные:**
- Файл `.credentials` — хранит все пароли локально
- Добавлен в `.gitignore` — не попадает в git
- Простой админ: `admin@admin.com` / `123456`

**База данных:**
- Очищена от тестовых данных
- Остался только 1 администратор

**Файлы:**
- `.credentials` — локальные пароли (не в git)
- `.gitignore` — обновлен для защиты секретов

---

### 4. 🐛 Исправлены проблемы

**Проблема 1: CORS ошибки**
- **Причина:** `Access-Control-Allow-Origin: *` + `credentials: true`
- **Решение:** Использовать конкретный origin: `http://recipes.local`

**Проблема 2: Network accessibility**
- **Причина:** Сервисы слушали только localhost
- **Решение:** Изменено на `0.0.0.0` для доступа по IP

**Проблема 3: API URL**
- **Причина:** Nuxt не читал .env переменную
- **Решение:** Создан `.env` с `NUXT_PUBLIC_API_BASE_URL`

---

### 5. 📚 Создана документация

**Файлы:**
- `USAGE.md` — как пользоваться проектом
- `SCRIPTS.md` — описание всех скриптов
- `LOCAL-DOMAIN.md` — детали про nginx и домены
- `START.md` — быстрый старт (обновлен)
- `QUICK-START.txt` — краткая шпаргалка (не в git)
- `.credentials` — учетные данные (не в git)
- `help.sh` — быстрая справка в терминале

---

## 🎯 Быстрый старт (каждый день)

```bash
# 1. Запустить все сервисы
npm run dev

# 2. Открыть приложение
open http://recipes.local

# 3. Зайти в админку
open http://admin.recipes.local
```

**Логин:**
```
Email:    admin@admin.com
Password: 123456
```

---

## 🛠 Полезные команды

```bash
# Посмотреть пароли
cat .credentials

# Быстрая справка
./help.sh

# Перезапустить все
./restart-dev.sh && npm run dev

# Перезапустить nginx
brew services restart nginx

# Сбросить базу данных
./reset-users.sh

# Создать нового админа
./create-admin.sh email@example.com username password
```

---

## 📱 Доступ с телефона

**В той же WiFi сети:**
```
http://192.168.1.14:3000  → Main App
http://192.168.1.14:5173  → Admin Panel
http://192.168.1.14:5010  → API
```

---

## 🔧 Архитектура nginx

```
Браузер: http://recipes.local
          ↓
/etc/hosts: recipes.local → 127.0.0.1
          ↓
nginx (порт 80):
  ├─ recipes.local/api/*  → proxy → localhost:5010 (API)
  ├─ recipes.local/*      → proxy → localhost:3000 (Nuxt)
  ├─ admin.recipes.local  → proxy → localhost:5173 (React)
  └─ api.recipes.local    → proxy → localhost:5010 (API)
          ↓
Dev серверы:
  ├─ .NET API (5010)
  ├─ Nuxt (3000)
  └─ React Admin (5173)
```

---

## ⚙️ Конфигурация

### Nuxt (.env)
```env
NUXT_PUBLIC_API_BASE_URL=http://recipes.local
```

### nginx
- Файл: `/opt/homebrew/etc/nginx/servers/recipes.conf`
- CORS: разрешен для `http://recipes.local`
- Проксирование API через `/api/*`

### Backend
- Слушает: `http://0.0.0.0:5010`
- База: PostgreSQL `recipes_db`
- CORS: настроен в Program.cs

---

## 🚨 Troubleshooting

### Страница не загружается
```bash
./restart-dev.sh
npm run dev
```

### nginx не работает
```bash
brew services restart nginx
nginx -t  # проверить конфиг
```

### CORS ошибки
- Проверь что nginx запущен
- Проверь что .env содержит `http://recipes.local`
- Сделай жесткую перезагрузку: ⌘ + Shift + R

### База данных
```bash
# Проверить PostgreSQL
brew services list | grep postgresql

# Сбросить базу
cd backend/RecipesApi
dotnet ef database drop --force
dotnet ef database update
```

---

## 📦 Что в git, что нет

**В git (публичное):**
- nginx-local.conf
- .env.example
- .gitignore
- Вся документация
- Исходный код

**НЕ в git (приватное):**
- .credentials (пароли)
- .env (конфигурация)
- QUICK-START.txt (шпаргалка)
- node_modules/
- bin/, obj/
- .nuxt/, .output/

---

## 🎓 Что узнали

1. **nginx reverse proxy** — как проксировать несколько сервисов
2. **CORS** — как правильно настроить с credentials
3. **Local domains** — как использовать .local домены
4. **Nuxt .env** — как настроить переменные окружения
5. **Monorepo** — как запускать несколько проектов вместе

---

## 🚀 Следующие шаги

- [ ] Добавить больше рецептов через админку
- [ ] Протестировать покупку/обмен рецептов
- [ ] Проверить работу на телефоне
- [ ] Настроить production деплой (Docker)

---

**Проект готов к разработке! 🎉**

Дата настройки: 2026-01-14
